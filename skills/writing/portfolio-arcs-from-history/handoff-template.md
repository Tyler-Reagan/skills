# Handoff template: portfolio arcs from history

Copy, fill, and pass to the next session. Skill: `/portfolio-arcs-from-history`
at `~/.claude/skills/portfolio-arcs-from-history/`.

```markdown
# Handoff: Portfolio arcs from history — {surface short name}

**Next session focus:** Invoke `/portfolio-arcs-from-history` for personal portfolio
substrate from live code + git history in `{repo}`.

**Repo(s):** `{absolute path(s)}`
**Output dir:** `{personal path — outside employer repo}`
**VCS user (for “my” MRs):** `{username}`

## Already done — do not redo

- {Taught spine / ADRs / prior substrate — or “none”}
- {Sibling arcs that must not be retold — paths}

## Goal

1. Unbiased search of merged MRs/issues/commits before proposing themes.
2. Organize by **skill theses**; count follows evidence.
3. Write dense `case-studies/` + skill-map README (**substrate** only).
4. Stop before interview cards — next skill is `/portfolio-swe-highlights`.

## Constraints

- Personal only; redact tenants / secrets / customer material.
- Optional shelves (Taught pages, ADRs) — never a gate.
- Bridge/consume to sibling arcs OK; do not retell their spines.
- No invented metrics.

## First actions

1. Read `~/.claude/skills/portfolio-arcs-from-history/SKILL.md`.
2. Confirm output dir + username + include/exclude themes.
3. Pull history without a pre-seeded theme list.
4. Draft skill map → case studies → README; stop.
```
